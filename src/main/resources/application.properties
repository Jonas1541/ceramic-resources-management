spring.application.name=Ceramic Resources Management System

api.security.token.secret=${JWT_SECRET:minhaChaveSecreta123}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=none

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

main.datasource.url=${MAIN_DB_URL:jdbc:mysql://localhost:3306/main_db?useSSL=false&allowPublicKeyRetrieval=true}
main.datasource.username=${MAIN_DB_USERNAME:root}
main.datasource.password=${MAIN_DB_PASSWORD:root}

tenant.datasource.base-url=${TENANT_DB_BASE_URL:jdbc:mysql://localhost:3306}
tenant.datasource.username=${TENANT_DB_USERNAME:root}
tenant.datasource.password=${TENANT_DB_PASSWORD:root}
tenant.datasource.port=${TENANT_DB_PORT:3306}

internal.job.auth.token=${INTERNAL_JOB_AUTH_TOKEN:um_valor_padrao_seguro_mas_melhor_definir_no_ambiente}

spring.jpa.properties.hibernate.cache.use_second_level_cache=false
spring.jpa.properties.hibernate.cache.use_query_cache=false

spring.jpa.open-in-view=false
